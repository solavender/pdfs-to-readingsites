---
name: pdf-reading-workflow
description: 将 PDF 经 Microsoft MarkItDown 提取为 Markdown，按需翻译或整理，在文本处理完成后询问四种阅读风格，再生成含高亮、批注、备份、对照与复习功能的 HTML 阅读器。用于 PDF 书籍、论文、教材转可阅读网页，或基于已提取文本延续此流程；不用于仅压缩、合并 PDF。
---

# PDF 阅读工作流

执行顺序：**PDF → MarkItDown → 原始 Markdown → 按需处理 → 询问四种风格 → HTML 阅读器 → 验证交付／按需发布**。

## 1. 接收材料与提取

- 根据当前任务定位用户指定 PDF；在有文件管理机制的环境中按其规则获取本地文件。
- 读取 [提取与转换](references/extraction.md)。仅使用 Microsoft MarkItDown 作为默认文本提取器；本工作流不使用 Adobe。MarkItDown 是外部 Python 转换工具，而非已内嵌在本 skill 中的服务。
- 优先运行 `scripts/extract_pdf.py INPUT.pdf OUTPUT_DIR`，按物理页提取并保存 `source.md`、`pages.json`、`manifest.json`。保存提取器版本、原文件 SHA-256 和实际物理页码；原刊／书内页码另行核对，不能按偏移量盲推。
- 扫描页、稀疏文字、多栏、表格、公式与图像须检查。默认转换不是可靠 OCR 或高保真排版重建；若扫描件需要 OCR，优先使用可用的本地 OCR，保留原件，再让 MarkItDown 转换可检索副本。没有 OCR 能力时说明缺失，不能把空白输出当作完整原文。不自动启用云端 OCR、付费服务或插件。
- 保留原始提取 Markdown。图像与图表须从 PDF 实际提取或渲染，并建立图号、图注与页面关联；Markdown 未输出图片不代表 PDF 没有图片。PDF 技能若可用，可辅助视觉核对及资产获取。

## 2. 按需处理文本

- 采用用户已明确的处理目标：完整翻译、语言保留、清理排版、整理或其他。不能把“全文翻译”改为摘要。用户未要求翻译时保留原语言；“处理一下”等实质目标不明时先问目标。
- 生成独立的 `processed.md`，保留 `source.md`，不得覆盖。长文按章节或页面分批处理，记录覆盖情况后合并，避免截断。
- 读取 [数据与溯源](references/data-model.md)，为段落分配稳定 ID，记录源页、原文和处理后文本的映射。保留引文、脚注、参考文献、公式和图表引用；统一术语，补充解释须独立标为译注／导读。
- 不把中文回译成英文后冒充原文，不虚构页码、漏失段落、图表或文献。
- 在进入下一步前保存文本与检查状态，保证等待用户选择期间可恢复。

## 3. 在文本处理完成后询问风格

读取 [四种风格](references/styles.md)，向用户展示 **全部四个选项**，并提供可访问的风格预览。可复制 `assets/style-preview.html` 作为本地预览；预览是演示，不是完整阅读器模板，不含持久化承诺。

推荐提问：

> 文本已处理完成。你希望采用哪种阅读器风格？
> 1. Notion / Arc 极简知识型
> 2. Apple Books 沉浸阅读型
> 3. Liquid Glass 玻璃质感型
> 4. Margin Notes 研究批注型

- 此环节属于用户明确要求的流程；一般在选定后再生成最终阅读器，不在刚导入 PDF 时提前询问。
- 用户已在当前任务明确选择或授权自行决定时，复用其选择，避免重复确认。无选择时保留检查点，等待回复，不私自认定其选了默认款。
- 若问答工具最多只容纳三个选项，用普通文字列出四项，不能删减或把第四款藏进“其他”。
- 用户只要求预览时，仅生成预览并停在其要求的范围，不擅自处理完整书籍或创建 skill。

## 4. 构建阅读器

读取 [功能与实现约束](references/reader-contract.md)。按所选风格呈现相同核心能力；沉浸式布局可将工具收进抽屉，不能删掉功能。

交付默认自包含、可离线打开的 HTML，使用实际处理结果。必备能力：

- 目录、全文搜索、阅读位置恢复、主题／字号／行距／栏宽设置；
- 多色高亮、创建／编辑／删除批注，稳定文本锚点；
- 自动保存与真实保存状态、数据备份导入／导出、含批注 HTML 导出；
- 章节／颜色／关键词筛选、返回原文、Markdown 与 Word 笔记导出；
- 原文与译文段落对照、核实的物理页及书内页码、附出处复制；
- 术语表及出现位置、图表放大与原图对照、译注及质量检查。

无可用原文、页码或原图时明确标示缺失并保留补充入口，不伪装已完成；不要把数据占位入口算作已完成的内容核对。未要求 AI 服务时，不添加假聊天入口、假解释生成或外部 API。

## 5. 验证与交付

- 读取 [验收清单](references/validation.md)，验证关键阅读行为、批注恢复、备份往返、内容覆盖和桌面／手机布局；修复观察到的问题。无法进行浏览器视觉检查时如实说明范围，不能声称已通过。
- 按环境规则保存输出。交付 HTML，并使 Markdown、图像、映射和检查点可恢复；不要为每个中间文件都增加用户下载负担。
- 用户要求网页／网址／Sites 时，使用当前环境的 `sites-building` 与 `sites-hosting` 技能，完成同一站点的保存、部署和成功状态检查；不要复制其内部命令、身份或凭据进此 skill。不可用时交付 HTML，说明尚未发布。
- 新站点遵循提供方默认访问范围；不因发布请求自行变为公开。保持已有站点访问范围。
- 发布 HTML 到网站不等于批注云同步。设备间迁移默认用备份；用户要求云同步时另按站点存储与身份规则实现。

## 依赖与资源边界

- **必需**：Python 3.10+、`markitdown[pdf]`；逐页辅助脚本另依赖 `pypdf`。
- **条件使用**：PDF 技能（资产／视觉核对）、文件管理技能（取用／保存）、documents（真正 DOCX 导出）、Sites（站点发布）、可用浏览器工具（交互及布局验证）。发现并读取目标环境实际存在的技能，不硬编码私人技能目录。
- 本包提供自己的 SOP、参考规范、提取脚本和风格演示；不捆绑或重新发布 Adobe、Sites 或其他平台的技能源码与认证信息。
